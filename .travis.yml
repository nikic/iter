dist: trusty
language: php

php:
  - 5.5
  - 5.6
  - 7.0
  - 7.1
  - 7.2
  - nightly
  - hhvm

matrix:
  allow_failures:
    - php: hhvm
    - php: nightly
  fast_finish: true

install:
  - composer install --prefer-dist

script:
  - vendor/bin/phpunit
